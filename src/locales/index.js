import { locales as localesComponents } from "../components";
import { locales as localesPages } from "../pages/locales";

export const locales = {
  pages: localesPages,
  components: localesComponents,
};
